"use client";

import { CommandMapWorkspace } from "./CommandMapWorkspace";

/** Command map block used on the dashboard overview (same implementation as the Workspace page). */
export function DashboardWideMapSection() {
  return <CommandMapWorkspace />;
}
